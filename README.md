# 开发
```bash
$ pnpm install # 安装仓库主体依赖
$ pnpm start # 运行vite项目

$ pnpm run core <other script> # 直接在core包下执行命令，如：npm run core install vuex -S，会在core下安装vuex
$ sh delete-all-node_modules # 可递归删除仓库所有node_modules目录
```
